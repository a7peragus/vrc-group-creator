<UserControl x:Class="VrcGroupCreator.Views.Controls.GroupCreationFormControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:VrcGroupCreator.Converters">
    <materialDesign:Card Padding="16" IsEnabled="{Binding IsLoggedIn}">
        <StackPanel>
            <TextBlock Text="Group Template" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16" />
            
            <TextBox materialDesign:HintAssist.Hint="Name Prefix" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding NamePrefix}" Margin="0,0,0,8" />
            
            <TextBox materialDesign:HintAssist.Hint="ShortCode (3-6 chars)"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding ShortCodePrefix}" Margin="0,0,0,8" />
            
            <TextBox materialDesign:HintAssist.Hint="Description" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding Description}" Margin="0,0,0,8" />
            
            <TextBox materialDesign:HintAssist.Hint="Count" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding GroupCount}" Margin="0,0,0,16" />

            <Button Content="Start Creation" Command="{Binding CreateGroupsCommand}" 
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}" />
        </StackPanel>
    </materialDesign:Card>
</UserControl>
