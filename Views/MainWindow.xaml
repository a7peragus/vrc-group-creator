<Window x:Class="VrcGroupCreator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:VrcGroupCreator.ViewModels"
        xmlns:controls="clr-namespace:VrcGroupCreator.Views.Controls"
        mc:Ignorable="d"
        Title="VRChat Group Creator" Height="700" Width="1200"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        WindowStyle="None" AllowsTransparency="False"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/app_icon.ico">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="64" GlassFrameThickness="0" CornerRadius="0" ResizeBorderThickness="8" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="WindowControlButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3FFFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="WindowCloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- DataContext is set via Dependency Injection in code-behind -->

    <materialDesign:DialogHost Identifier="RootDialog" SnackbarMessageQueue="{Binding Notifications}">
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header / Custom Title Bar -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="0,0,0,0" Height="64">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="16,0,0,0">
                    <materialDesign:PackIcon Kind="AccountGroup" Width="32" Height="32" VerticalAlignment="Center" />
                    <TextBlock Text="VRChat Group Creator" Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                               Margin="16,0,0,0" VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Style="{StaticResource WindowControlButton}" Command="{Binding ShowSettingsCommand}" ToolTip="GUI Settings">
                        <materialDesign:PackIcon Kind="Settings" Width="18" Height="18" />
                    </Button>
                    <Button Style="{StaticResource WindowControlButton}" Click="BtnMinimize_Click">
                        <materialDesign:PackIcon Kind="Minus" Width="18" Height="18" />
                    </Button>
                    <Button Style="{StaticResource WindowControlButton}" Click="BtnMaximize_Click">
                        <materialDesign:PackIcon Kind="WindowMaximize" Width="18" Height="18" />
                    </Button>
                    <Button Style="{StaticResource WindowCloseButton}" Click="BtnClose_Click">
                        <materialDesign:PackIcon Kind="Close" Width="18" Height="18" />
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>

        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="350" />
            </Grid.ColumnDefinitions>

            <!-- Settings Side -->
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <controls:AccountSelectorControl />
                <controls:GroupCreationFormControl />
            </StackPanel>

            <!-- Logs & Progress Side -->
            <Grid Grid.Column="1" Margin="8,0,0,0">
                <controls:ActionLogControl />
            </Grid>

            <!-- Groups List Side -->
            <Grid Grid.Column="2" Margin="8,0,0,0">
                <controls:GroupListControl />
            </Grid>
        </Grid>
        <materialDesign:Snackbar MessageQueue="{Binding Notifications}" Grid.RowSpan="2" VerticalAlignment="Bottom" />
    </Grid>
</materialDesign:DialogHost>
</Window>
